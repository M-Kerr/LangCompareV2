Verify the timing results received are correct

Implement iterations

Add a timing results base unit option (seconds, ns, ms, etc).
    - Add to documentation

Implement sockets
    - Update README to reflect sockets instead of pipes

Add markdown to README.md

Implement GUI

Add licensing

Make user-code compile/run into a code/tmp directory
    - Currently builds into and clutters application directory

Implement geohot's multi-language algorithm repo

Add Qt src to maintain Qt licensing requirements

Allow user to define python environment

Add compiler support for MSVC and GCC
